#include <stdio.h>
#include <stdlib.h>

int main()
{
  int i = 10;
  unsigned short int a;
  double u;

  printf("i\t%i bytes\n", sizeof(i));
  printf("a\t%i bytes\n", sizeof(a));
  printf("u\t%i bytes\n", sizeof(u));
  

return 0;
}